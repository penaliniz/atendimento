# Ignore environment files for the server (contain secrets)
 /server/.env
 *.env
.env
# Optional local env overrides
 /server/.env.local

# REGRAS PARA NODE_MODULES E DEPENDÊNCIAS
/agent/node_modules
/server/node_modules
node_modules/ # Captura qualquer node_modules que possa aparecer na raiz ou subpasta